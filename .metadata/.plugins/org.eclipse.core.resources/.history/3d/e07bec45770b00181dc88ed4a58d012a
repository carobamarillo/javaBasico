<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
  <head>
    <title>Index</title>
  </head>
  <body>
    <h1>Listado de libros</h1>
    
    <table>
    <thead>
    	<tr>
    		<th> Titulo</th>
    	</tr>
    </thead>
    <tbody>
    	<tr th:each = "libro : ${book}">
    		<td th:text="${libro.getName()}" ></td>
    		<td> <a 
    		th:href="@{/delete(id=${book.getIdBook()})}">
    		 delete </a>  
    		 </td> 	
    	</tr>
    </tbody>
    </table>
    
    
    <p th:text = "'Nombre: ' + ${book.getName()}  
    + ${book.getBookDetails().getAutor()}"></p>
	<a class="btn btn-danger" th:href="@{/delete(id=${book.getIdBook()})}"> delete </a>
  </body>
</html>