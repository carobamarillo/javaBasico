<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<title>Index</title>
<meta name="viewport"
	content="width=device-width, initial-scale=1, shrink-to-fit=no"></meta>
</head>
<body>
	<form th:action="crearOrdenTrabajo" th:object="${ordenDeTrabajo}"
		method="post">
		<label>Nro patente </label>
		 <input type="number" style="margin-left: 1em" th:field="*{patente}"
			placeholder="Ingrese numero" />
		<label>Marca </label>
		 <input type="text" style="margin-left: 1em" th:field="*{marca}"
			placeholder="Ingrese numero" />
		<label>Detalle falla </label>
			 <textarea th:field="*{detalle}" rows="4" cols="50">
			 </textarea> 
		<label>Datos propietario</label>
		<input type="text" style="margin-left: 1em" th:field="*{}"
			placeholder="Ingrese numero" />
		<input type="text" style="margin-left: 1em" th:field="*{marca}"
			placeholder="Ingrese numero" />
		<input type="text" style="margin-left: 1em" th:field="*{marca}"
			placeholder="Ingrese numero" />
	</form>
</body>
</html>